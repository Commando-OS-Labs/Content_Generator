{
  "name": "commando360-ad-generator",
  "version": "1.0.0",
  "description": "Figma plugin to bulk-generate ad variations from copy JSON",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "typescript": "^5.4.0"
  }
}
